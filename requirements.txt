# --- Core FastAPI and Server ---
fastapi==0.111.0              # Web framework for building APIs quickly, async support
uvicorn[standard]==0.30.1     # ASGI server with auto-reload, websockets, httptools, and more

# --- Environment and Settings ---
python-dotenv==1.0.1          # Loads environment variables from .env
pydantic-settings==2.10.1     # Environment file reading

# --- Database ---
psycopg2-binary==2.9.10       # PostgreSQL driver for SQLAlchemy
SQLAlchemy==2.0.41            # PostgreSQL ORM
alembic==1.16.2               # Database migrations

# --- Background Tasks ---
celery==5.4.0                 # Distributed task queue
redis==5.0.3                  # Redis client for Celery

# --- File Uploads ---
python-multipart==0.0.9       # Required for form parsing (file/image upload)
aiofiles==23.2.1              # Async file handling (needed for FastAPI file uploads)

# --- HTTP Client ---
httpx==0.27.0                 # Async HTTP client

# --- Encoding/Decoding ---
PyJWT==2.8.0                  # JSON Web Token implementation

# --- Image Processing (Essential) ---    
pillow==11.2.1                # Image processing
pillow-heif==0.20.0           # HEIC/HEIF support for Pillow
pillow-avif-plugin==1.4.3     # AVIF support for Pillow
numpy==1.26.4                 # Numerical computing (required by image processing)
imageio==2.36.0               # Image I/O library

# --- Audio Processing (Basic) ---
pydub==0.25.1                 # Audio manipulation and conversion
mutagen==1.47.0               # Audio metadata reading/writing

# --- Video Processing (Basic) ---
ffmpeg-python==0.2.0          # FFmpeg bindings for basic video processing

# --- Development Tools ---
watchdog==4.0.0               # For dev-time hot-reload

# --- Production Server ---
gunicorn==21.2.0              # Production WSGI server
supervisor==4.2.5             # Process manager for running multiple services
